<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ecnu.chinesecharactercottage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Activitys.BeginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"></action>
                <category android:name="android.intent.category.LAUNCHER"></category>
            </intent-filter>
        </activity>

        <activity android:name=".Activitys.CCCMainActivity"></activity>
        <activity android:name=".Activitys.HSKActivity"></activity>
        <activity android:name=".Activitys.ComponentListActivity"></activity>
        <activity android:name=".Activitys.ExampleActivity"></activity>
        <activity android:name=".Activitys.ReviewActivity"></activity>

    </application>

</manifest>
